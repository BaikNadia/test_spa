# 🛍️ Products SPA 
## 🎯 Описание проекта
Products SPA - это одностраничное приложение (Single Page Application) для управления каталогом продуктов. Приложение позволяет просматривать, добавлять, редактировать и удалять продукты, а также помечать их как избранные.

Основные концепции:
SPA (Single Page Application) - все взаимодействия происходят без перезагрузки страницы

Client-side rendering - рендеринг на стороне клиента с помощью React

State management - централизованное управление состоянием через Zustand

TypeScript - строгая типизация для надежности кода

## ✨ Функциональность
✅ Реализованные функции:
🏠 Главная страница (/)
Отображение списка продуктов в виде карточек

Поиск продуктов по названию и описанию (в реальном времени)

Фильтрация: все продукты / только избранные

Действия с карточками:

❤️ Лайк/анлайк продукта

🗑️ Удаление продукта

📱 Клик по карточке → переход на детальную страницу

📄 Страница продукта (/products/[id])
Подробная информация о продукте

Полноразмерное изображение

Статус лайка с возможностью изменения

Кнопка возврата на главную страницу

➕ Создание продукта (/create-product)
Форма создания нового продукта

Валидация полей:

Название (обязательное, мин. 3 символа)

Описание (обязательное, мин. 10 символов)

Цена (обязательное, больше 0)

URL изображения (обязательное, валидный URL)

Превью изображения перед созданием

## 🛠 Технологический стэк
Основные технологии:
Next.js 16 - React фреймворк с App Router

TypeScript - типизированный JavaScript

Tailwind CSS - утилитарный CSS фреймворк

Zustand - управление состоянием

Дополнительные библиотеки:
React Hook Form - управление формами

Zod - валидация схем

Lucide React - иконки

ESLint - линтинг кода
Версии:
{
  "next": "16.0.3",
  "react": "19.2.0", 
  "typescript": "^5",
  "zustand": "^4.4.0",
  "tailwindcss": "^4"
}

## 🚀 Установка и запуск
Предварительные требования:
Node.js 18.x или выше

npm или yarn

Пошаговая установка:
Клонирование репозитория (если есть Git)

bash
git clone <repository-url>
cd test-spa
Установка зависимостей

bash
npm install
Запуск в режиме разработки

bash
npm run dev
Открытие в браузере

text
http://localhost:3000
Другие npm скрипты:
bash
 Production сборка:
npm run build

 Запуск production версии:
npm start

 Проверка кода:
npm run lint

 TypeScript проверка:
npx tsc --noEmit

## 📁 Структура проекта:
test-spa/
├── src/
│   ├── app/                    # Страницы Next.js (App Router)
│   │   ├── products/
│   │   │   └── [id]/
│   │   │       └── page.tsx    # Динамическая страница продукта
│   │   ├── create-product/
│   │   │   └── page.tsx        # Страница создания продукта
│   │   ├── layout.tsx          # Корневой layout
│   │   ├── page.tsx            # Главная страница
│   │   └── globals.css         # Глобальные стили
│   ├── components/             # React компоненты
│   │   ├── ProductCard.tsx     # Карточка продукта
│   │   └── ProductList.tsx     # Список продуктов
│   ├── stores/                 # Zustand stores
│   │   └── productStore.ts     # Store для продуктов
│   ├── types/                  # TypeScript типы
│   │   └── product.ts          # Интерфейсы продуктов
│   └── utils/                  # Вспомогательные функции
│       ├── api.ts              # API функции
│       └── mockData.ts         # Моковые данные
├── public/                     # Статические файлы
├── package.json
├── tailwind.config.ts
├── tsconfig.json
└── next.config.ts

## 🧩 Компоненты
ProductCard (/src/components/ProductCard.tsx)
Назначение: Отображение отдельной карточки продукта

Props:

typescript
interface ProductCardProps {
  product: Product;
}
Функциональность:

Отображение изображения, названия, описания, цены

Кнопки лайка и удаления

Навигация на страницу продукта при клике

Адаптивный дизайн

ProductList (/src/components/ProductList.tsx)
Назначение: Отображение сетки продуктов

Props:

typescript
interface ProductListProps {
  products: Product[];
}
Функциональность:

Сетка карточек (адаптивная: 1-4 колонки)

Сообщение при отсутствии продуктов

Key prop для оптимизации React

## 💾 Store (Zustand)
Product Store (/src/stores/productStore.ts)
Состояние:

typescript
interface ProductState {
  products: Product[];
  // Вычисляемые свойства...
}
Действия:

addProduct() - добавление нового продукта

updateProduct() - обновление существующего продукта

deleteProduct() - удаление продукта

toggleLike() - переключение лайка

getProductById() - получение продукта по ID

searchProducts() - поиск продуктов

Persist middleware:

Автоматическое сохранение в localStorage

Восстановление состояния при перезагрузке

## 🌐 API и данные
Модель данных:
interface Product {
  id: number;           // Уникальный идентификатор
  title: string;        // Название продукта
  description: string;  // Описание
  price: number;        // Цена
  image: string;        // URL изображения
  isLiked: boolean;     // Статус лайка
  createdAt?: string;   // Дата создания
}
Источники данных:
JSONPlaceholder API - первоначальная загрузка

Mock данные - резервный источник

Локальное создание - пользовательские продукты

API функции:
fetchProducts() - загрузка продуктов

Автоматический fallback на mock данные при ошибках

